# 伪装成 Windows 7 上的 IE 9 - 在特定时候我会开启此选项，但大多数时候我并不想使用
# user-agent = "Mozilla/5.0 (compatible; MSIE 9.0; Windows NT 6.1; Trident/5.0)"

# 重定向时将上一个 URL 设为 referer
referer = ";auto"

# 超时前等待 60 s
connect-timeout = 60
